{
  "name": "geo-location-attendance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:env": "node scripts/generate-env.mjs",
    "netlify:dev": "NETLIFY_EDGE_BOOTSTRAP=file://$PWD/netlify/dev-edge-bootstrap.ts netlify dev",
    "build": "rm -rf build && vite build --outDir build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "firebase": "^12.17.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.14.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "netlify-cli": "^15.0.0",
    "vite": "^5.3.0"
  }
}
